class Button {
private label: string;
private onClick: () => void;
private size: p5.Vector

    constructor(label: string, onClick: () => void, size: creaeVector()) {
    this.label = label;
    this.onClick = onClick;
    this.size = size;
    }

    
}


